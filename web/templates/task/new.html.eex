<h2>New task</h2>

<%= render "form.html", changeset: @changeset, category_list: @category_list,
                        action: task_path(@conn, :create) %>

<%= link "Back", to: task_path(@conn, :index) %>
